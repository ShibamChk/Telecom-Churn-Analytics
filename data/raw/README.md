# Raw Data

Place the original telecom customer churn dataset files in this directory manually.

Raw data files are ignored by Git because they may be large, contain sensitive information, or have license restrictions. Keep the original files unchanged so the cleaning process remains reproducible.

The cleaning script searches this directory for CSV files:

```powershell
python src/clean_data.py
```
