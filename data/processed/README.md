# Processed Data

Cleaned and dashboard-ready CSV files generated by the project scripts are saved here.

Expected outputs include:

- `cleaned_customers.csv`
- `fact_customers.csv`
- Available segment summary tables
- `revenue_risk_summary.csv`
- `retention_curve_table.csv`

Generated CSV files are ignored by Git and can be recreated from the raw dataset.
